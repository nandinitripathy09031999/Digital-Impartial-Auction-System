export const setDisabled = payload =>({
    type:'SET_DISABLE_STATE',
    payload
})

export const setIdxSelected = payload =>({
    type:'SET_IDX_SELECTED',
    payload,
})

export const displayProfessor = payload =>({
    type:'DISPLAY_PROFESSOR',
    payload
})